#include<stdio.h>
#include<vector>
#include<utility>
#include<algorithm>

using namespace std;

vector <int> metals;
vector <pair<int, int>> recip;

int makeit(int n) {
	if()
}

int main() {
	int t;
	scanf("%d", &t);
	for (int i = 0; i < t; ++i) {
		int m;
		int a, b;
		
		scanf("%d", &m);
		for (int j = 0; j < m; ++j) {
			scanf("%d %d", &a, &b);
			recip.push_back(make_pair(a, b));
		}
		for (int j = 0; j < m; ++j) {
			scanf("%d", a);
			metals.push_back(a);
		}
	}
}