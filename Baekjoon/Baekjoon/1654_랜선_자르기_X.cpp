#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main() {
	int k, n, tmp;
	vector <int> len;
	scanf("%d %d", &k, &n);
	for (int i = 0; i < k; i++) {
		scanf("%d", &tmp);
		len.push_back(tmp);
	}

}